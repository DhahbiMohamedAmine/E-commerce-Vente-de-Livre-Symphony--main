{% extends 'base.html.twig' %}

{% block title %}Hello CategorieController!{% endblock %}

{% block body %}
    <h1>Liste des Catégories</h1>
   <table class="table">
    <tr>
        <th>libelle</th>
        <th>description</th>
        <th colspan="3">Action</th>
    </tr>
    {% for categorie in categories %}
        <tr>
            <td>{{categorie.libelle}}</td>
            <td>{{categorie.description}}</td>
            <td><a href="{{ path('admin_categorie_show', {'id': categorie.id}) }}" class="btn btn-primary btn-sm">Voir plus ....</a></td>
            <td><a href="{{ path('admin_categorie_delete', {'id': categorie.id}) }}" class="btn btn-danger btn-sm">Supprimer</a></td>
            <td><a href="{{ path('admin_categorie_update', {'id': categorie.id}) }}" class="btn btn-success btn-sm">Editer</a></td>
        </tr>
    {% endfor %}
</table>
    <a href="{{ path('admin_categorie_create') }}" class="btn btn-primary btn-sm">ajouter</a>
{% endblock %}
